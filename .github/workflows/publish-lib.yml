name: publish-lib

on:
  workflow_dispatch:
  push:
    tags:
      - "r-lanlib-v*"

jobs:
  publish:
    name: Publish r-lanlib crate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/rust-setup
      - uses: katyo/publish-crates@v2
        with:
          path: ./lib
          registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
