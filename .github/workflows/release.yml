name: gh-release

on:
  push:
    branches: ["main"]

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./.github/actions/releasaurus
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
