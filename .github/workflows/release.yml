name: gh-release

on:
  push:
    branches: ["main"]

jobs:
  releasaurus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: robgonnella/releasaurus-action@v2
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
