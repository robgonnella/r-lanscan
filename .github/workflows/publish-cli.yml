name: publish-cli

on:
  workflow_dispatch:
  push:
    tags:
      - "r-lancli-v*"

jobs:
  publish:
    name: Publish r-lancli crate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/rust-setup
      - uses: katyo/publish-crates@v2
        with:
          path: cli
          registry-token: ${{ secrets.CARGO_REGISTRY_TOKEN }}
